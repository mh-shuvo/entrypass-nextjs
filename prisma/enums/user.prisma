enum UserType {
  SUPERADMIN
  EXECUTIVE
}

enum UserStatus {
  ACTIVE
  INACTIVE
  BLOCKED
}

enum Permission {
  manage_events
  manage_user
  manage_own_profile
}

enum ExecutivePermission {
  manage_events
  manage_own_profile
}
